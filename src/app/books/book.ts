export class Book {
  constructor(public isbn: string, public bookName: string, public type: string, public author: string, public startDate: string, public completeDate: string, public revision: boolean){

  }
}
